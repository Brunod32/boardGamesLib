{{ form_start(form) }}
    {{ form_row(form.name, {'label': 'Nom'}) }}
    <button type="button" class="btn btn-outline-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
