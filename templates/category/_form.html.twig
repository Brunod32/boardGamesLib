{{ form_start(form) }}
    {{ form_row(form.name, {'label': 'Nom de la catégorie'}) }}
    <button type="button" class="btn btn-outline-primary mt-5 mb-5">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
