<form method="post" action="{{ path('app_nb_player_delete', {'id': nb_player.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nb_player.id) }}">
    <button class="btn">Supprimer</button>
</form>
