<form method="post" action="{{ path('app_nb_player_delete', {'id': nb_player.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nb_player.id) }}">
    <button class="btn">Delete</button>
</form>
